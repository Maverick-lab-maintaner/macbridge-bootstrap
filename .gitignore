# MacBridge bootstrap logs — keep directory, ignore contents
logs/*.log
logs/*
!logs/.gitkeep

# Watcher state files (version tracking cache)
.cache/

# Go build artifacts (binary only, not source directory)
/macbridge
/macbridge.exe
